# machine_learning
linear regression, logistic regression with synthetic dataset,   logistic regression with iris dataset, svm on iris dataset, knn mnist dataset, decision tree, random forest, k-means clustering
